name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Setup Pages
      uses: actions/configure-pages@v4
    
    - name: Build site
      run: |
        # Copy all files to build directory
        mkdir -p _site
        cp -r * _site/ 2>/dev/null || true
        
        # Remove git directory and github workflows from build
        rm -rf _site/.git _site/.github
        
        # Ensure CNAME file is present for custom domain
        echo "uaicp.org" > _site/CNAME
        
        # Create .nojekyll to prevent Jekyll processing
        touch _site/.nojekyll
        
        echo "Site built successfully"
        ls -la _site/
    
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: '_site'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    
    steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
      
    - name: Output deployment URL
      run: |
        echo "🚀 Site deployed to: ${{ steps.deployment.outputs.page_url }}"
        echo "🌐 Custom domain: https://uaicp.org"
